# 元件参考手册

本文是 [使用指南](./USAGE.md) 的附件，列出扩展当前支持的全部元件。

## 端口约定

- `输入`表示信号进入元件，`输出`表示元件计算结果离开元件，`无源`表示端口本身不驱动网络。
- 未连接的输入端默认为 `0`。
- 端口名称不区分大小写，例如 `OUT` 和 `out` 等价。
- 元件 ID 严格区分大小写；`inputA` 和 `InputA` 是两个不同元件。
- 元件类型只接受本手册和菜单列出的规范英文名称，不接受别名或中文类型名。
- 只有相同位宽的端口可以连接。
- 1 位信号范围为 `0..1`；2、4、8 位总线分别为 `0..3`、`0..15`、`0..255`。
- 每个输入、输出或无源端口都可以连接任意数量的线路。
- 普通端口只能按输出到输入连接；输入不能直连输入，输出不能直连输出。
- `VIA.IO` 可连接输入、输出或另一个无源端口，但同一个 Net 仍只能有一个输出驱动源。
- 每条线路都有唯一 `lineId`；相同两个端口之间不能建立重复线路。

## 线路过孔

| 类型名称 | 无源端口 | 位宽 | 作用 |
| --- | --- | --- | --- |
| `VIA` | `IO` | 自动 `0/1/2/4/8` | 单点线路交汇。任意数量分支连接同一个 `IO`；未确定宽度时为 0。 |

`VIA` 不区分输入和输出，不执行逻辑计算，也不增加传播节拍。它忽略添加积木中的位宽且
不支持手动修改；接入不同位宽或多个真实驱动源时会报告错误。没有 VIA 的几何交叉仍是
相互独立的网络。

## 关卡接口与信号源

| 类型名称 | 输入端口 | 输出端口 | 作用 |
| --- | --- | --- | --- |
| `LEVEL_INPUT` | 无 | `OUT`（可选 1/2/4/8 位） | 关卡验证输入。验证器按输入 JSON 写入 `OUT`。 |
| `LEVEL_OUTPUT` | `IN`（可选 1/2/4/8 位） | 无 | 关卡验证输出。验证器从 `IN` 采样。 |
| `SWITCH` | `A`（可选位宽），`S`（1 位） | `OUT`（与 A 同宽） | 受控开关。`S=1` 时 `OUT=A`，`S=0` 时 `OUT=0`。 |
| `ALWAYS_ON` | 无 | `OUT`（可选位宽） | 每一位始终为 `1`。 |
| `ALWAYS_OFF` | 无 | `OUT`（可选位宽） | 每一位始终为 `0`。 |

`SWITCH` 真值表：

| A | S | OUT |
| --- | --- | --- |
| 0 | 0 | 0 |
| 1 | 0 | 0 |
| 0 | 1 | 0 |
| 1 | 1 | 1 |

## 基础逻辑门

| 类型名称 | 输入端口 | 输出端口 | 逻辑 |
| --- | --- | --- | --- |
| `NOT` | `A` | `OUT` | `NOT A` |
| `AND` | `A`, `B` | `OUT` | `A AND B` |
| `OR` | `A`, `B` | `OUT` | `A OR B` |
| `NAND` | `A`, `B` | `OUT` | `NOT(A AND B)` |
| `NOR` | `A`, `B` | `OUT` | `NOT(A OR B)` |
| `XOR` | `A`, `B` | `OUT` | A、B 不同时为 `1` |
| `XNOR` | `A`, `B` | `OUT` | A、B 相同时为 `1` |

注册时可选择 1、2、4 或 8 位；同一个门的所有数据端口使用相同位宽，并逐位运算。

## 多输入逻辑门

三输入门使用 `A`、`B`、`C`，四输入门使用 `A`、`B`、`C`、`D`，输出端均为
`OUT`。注册时可选择 1、2、4 或 8 位，并对各位并行执行相同逻辑。

| 类型名称 | 作用 |
| --- | --- |
| `3AND`, `4AND` | 所有输入均为 `1` 时输出 `1`。 |
| `3OR`, `4OR` | 任意输入为 `1` 时输出 `1`。 |
| `3NAND`, `4NAND` | AND 结果取反。 |
| `3NOR`, `4NOR` | OR 结果取反。 |
| `3XOR`, `4XOR` | 输入中 `1` 的数量为奇数时输出 `1`。 |
| `3XNOR`, `4XNOR` | XOR 结果取反。 |

## 复合逻辑元件

| 类型名称 | 输入端口 | 输出端口 | 作用 |
| --- | --- | --- | --- |
| `HALF_ADDER` | `A`, `B` | `SUM`, `CARRY` | 半加器：计算两个 1 位数相加。 |
| `FULL_ADDER` | `A`, `B`, `CIN` | `SUM`, `COUT` | 全加器：将两个 1 位数和输入进位相加。 |
| `MUX` | `A`, `B`, `SEL` | `OUT` | `SEL=0` 选择 A，`SEL=1` 选择 B。 |
| `AOI` | `A`, `B`, `C` | `OUT` | `NOT((A AND B) OR C)`。 |
| `OAI` | `A`, `B`, `C` | `OUT` | `NOT((A OR B) AND C)`。 |

`HALF_ADDER` 和 `FULL_ADDER` 固定为 1 位。MUX 的 A/B/OUT 以及 AOI/OAI 的数据端口可选
1、2、4、8 位；MUX.SEL 固定为 1 位。

## 总线加法器

| 类型名称 | 输入端口 | 输出端口 | 作用 |
| --- | --- | --- | --- |
| `ADDER4` | `A`, `B`（4 位），`CIN`（1 位） | `SUM`（4 位），`COUT`（1 位） | 计算 `A+B+CIN`。 |
| `ADDER8` | `A`, `B`（8 位），`CIN`（1 位） | `SUM`（8 位），`COUT`（1 位） | 计算 `A+B+CIN`。 |

溢出的最高位写入 `COUT`。

## 分线器与集线器

| 类型名称 | 输入端口 | 输出端口 | 作用 |
| --- | --- | --- | --- |
| `SPLITTER` | `IN`（8 位） | `B0..B7`（各 1 位） | 将字节拆成 8 根单线，`B0` 是最低位。 |
| `HUB` | `B0..B7`（各 1 位） | `OUT`（8 位） | 将 8 根单线组合成一个字节。 |
| `SPLITTER2` | `IN`（2 位） | `B0`, `B1`（各 1 位） | 将 2 位值拆成两根单线。 |
| `HUB2` | `B0`, `B1`（各 1 位） | `OUT`（2 位） | 将两根单线组合成 2 位值。 |
| `SPLITTER4` | `IN`（4 位） | `B0..B3`（各 1 位） | 将 4 位值拆成四根单线。 |
| `HUB4` | `B0..B3`（各 1 位） | `OUT`（4 位） | 将四根单线组合成 4 位值。 |

### 8 位分组转换器

以下转换器都保存同一个 8 位值，只改变端口数量和每个端口的位宽。统一使用
`IN0...` 和 `OUT0...`；编号越小，位权越低。

| 类型名称 | 输入端口 | 输出端口 |
| --- | --- | --- |
| `CONVERTER_2_TO_8` | `IN0..IN1`（各 4 位） | `OUT0..OUT7`（各 1 位） |
| `CONVERTER_8_TO_2` | `IN0..IN7`（各 1 位） | `OUT0..OUT1`（各 4 位） |
| `CONVERTER_4_TO_2` | `IN0..IN3`（各 2 位） | `OUT0..OUT1`（各 4 位） |
| `CONVERTER_2_TO_4` | `IN0..IN1`（各 4 位） | `OUT0..OUT3`（各 2 位） |
| `CONVERTER_4_TO_8` | `IN0..IN3`（各 2 位） | `OUT0..OUT7`（各 1 位） |
| `CONVERTER_8_TO_4` | `IN0..IN7`（各 1 位） | `OUT0..OUT3`（各 2 位） |

例如 `CONVERTER_2_TO_8` 中，`IN0` 提供低四位、`IN1` 提供高四位；其 `OUT0` 是整个值的
最低位，`OUT7` 是最高位。`CONVERTER_8_TO_2` 按相反规则还原。

## 时序与存储元件

| 类型名称 | 输入端口 | 输出端口 | 作用 |
| --- | --- | --- | --- |
| `REGISTER` | `D`（8 位），`CLK`（1 位） | `Q`（8 位） | 在 `CLK` 的 `0 -> 1` 上升沿把 D 锁存到 Q。 |
| `ROM` | `ADDR`（8 位） | `DATA`（8 位） | 读取 256 字节只读存储器；默认全为 0。 |
| `RAM` | `ADDR`, `DIN`（8 位），`WE`, `CLK`（1 位） | `DOUT`（8 位） | 异步读取；`WE=1` 时在 `CLK` 上升沿写入。 |

保持 `CLK=1` 重复 Tick 不会再次触发，必须先回到 `0` 再升到 `1`。

## 通用边界元件

这些类型主要用于手动调试、连接总线和 JavaScript API。普通关卡优先使用
`LEVEL_INPUT` 与 `LEVEL_OUTPUT`。

| 类型名称 | 端口 | 作用 |
| --- | --- | --- |
| `INPUT` | 输出 `OUT`（1 位） | 通用 1 位输入源。 |
| `OUTPUT` | 输入 `IN`（1 位） | 通用 1 位采样端。 |
| `BUS2_INPUT` / `BUS2_OUTPUT` | `OUT` / `IN`（2 位） | 2 位总线边界。 |
| `BUS4_INPUT` / `BUS4_OUTPUT` | `OUT` / `IN`（4 位） | 4 位总线边界。 |
| `BUS_INPUT` / `BUS_OUTPUT` | `OUT` / `IN`（8 位） | 8 位总线边界。 |
