# bsen975-de-logic-gate 使用指南

这份指南先解释扩展里的几个概念，再带你完整搭建并验证一个 NOT 门。

扩展顶部的“打开使用指南”按钮会在编辑器内显示一份可离线使用的快速指南；点击右上角
关闭按钮、弹窗外区域或按 `Esc` 均可关闭。

完整元件名称、作用、端口和位宽请查看附件：[元件参考手册](./COMPONENTS.md)。

## 先理解四个概念

### 1. 核心

“启动电路模拟器”会创建保存电路和执行计算的引擎。在支持 Web Worker 的
Gandi/TurboWarp 环境中，引擎运行在独立线程。

只有启动模拟器后，添加、连接、读取和验证积木才会工作。“关闭电路模拟器”会删除
整个运行中的电路并释放线程。

### 2. 元件 ID

每个元件都必须有一个不重复的 ID，例如：

- 输入端：`Level_Input`
- NOT 门：`not1`
- 输出端：`Level_Output`

连接、读取端口和验证时使用的是 ID，不是角色名称。ID 严格区分大小写，
`Level_input`、`Level_Input` 和 `level_input` 是三个不同的 ID。注册、连接、设置电平、
读取状态以及关卡 JSON 必须始终使用完全一致的拼写。

### 3. 端口

连接时需要填写元件两端的端口名称。例如：

```text
Level_Input.OUT -> not1.A
not1.OUT -> Level_Output.IN
```

端口名区分用途，但输入时不区分大小写。常见规则：

- `LEVEL_INPUT`：输出端是 `OUT`。
- `LEVEL_OUTPUT`：输入端是 `IN`。
- `VIA`：只有无源端口 `IO`，所有交汇分支都连接这个端口。
- NOT：输入端是 `A`，输出端是 `OUT`。
- 两输入门：输入端是 `A`、`B`，输出端是 `OUT`。
- `SWITCH`：数据输入是 `A`，控制输入是 `S`，输出是 `OUT`。

### 4. LEVEL_INPUT、SWITCH 和 LEVEL_OUTPUT

这三个元件的职责不同：

```text
数据源 -----------> SWITCH.A
控制信号 ---------> SWITCH.S -> SWITCH.OUT -> 其他逻辑门
最终信号 -> Level_Output.IN -> 自动化验证读取
```

- `LEVEL_INPUT` 是信号源。自动化验证会按照输入 JSON 设置它，也可使用
  “将输入设为”手动设置。
- `SWITCH` 不是信号源。它的规则是 `OUT = S ? A : 0`：`S=1` 时把 `A` 原样送到
  `OUT`，`S=0` 时无论 `A` 是什么都输出 `0`。
- `LEVEL_OUTPUT` 是关卡采样点。验证器从它的 `IN` 端读取结果。

## 示例一：从零搭建 NOT 门

目标真值表：

| 输入 | 输出 |
| --- | --- |
| 0 | 1 |
| 1 | 0 |

### 第一步：初始化

依次执行：

```text
启动电路模拟器
清空当前电路
```

### 第二步：注册三个元件

执行三次“添加元件”积木：

```text
添加 [LEVEL_INPUT] 元件 ID [Level_Input] 位宽 [1] 绑定 [不绑定]
添加 [NOT] 元件 ID [not1] 位宽 [1] 绑定 [不绑定]
添加 [LEVEL_OUTPUT] 元件 ID [Level_Output] 位宽 [1] 绑定 [不绑定]
```

这里选择“不绑定”，因为示例不需要把元件绑定到某个角色或克隆体。

### 第三步：连接端口

执行两次“连接线路”积木，并给每条线路一个供前端绘制使用的稳定 ID：

```text
连接线路 ID [wire-input]：[Level_Input].[OUT] 到 [not1].[A]
连接线路 ID [wire-output]：[not1].[OUT] 到 [Level_Output].[IN]
```

此时电路是：

```text
Level_Input.OUT -> not1.A -> not1.OUT -> Level_Output.IN
```

可以使用“全部线路 JSON”，或把“当前电路的数据 JSON”切换到“运行状态”，检查这两条连接。

### 第四步：手动试运行

执行：

```text
将输入 [Level_Input] 设为 [0]
以 [60] Hz 传播电路直到稳定
端口 [Level_Output].[IN] 的值
```

应该得到 `1`。再把输入电平设为 `1`，以所需 Hz 传播电路直到稳定，输出应该变成 `0`。

修改输入之后应执行“以 Hz 传播电路直到稳定”，确保关系图中所有下游元件完成计算。

### 第五步：运行自动化验证

`LEVEL_INPUT` 和 `LEVEL_OUTPUT` 注册后已经自动成为验证接口，不需要额外配置。载入数据：

```json
输入列表JSON：[{"Level_Input":0},{"Level_Input":1}]
期望列表JSON：[{"Level_Output":1},{"Level_Output":0}]
模式：每组重置
```

执行：

```text
以 [60] Hz 运行全部测试用例
测试结果 JSON
```

正确结果为：

```json
{"passed":true,"total":2,"passedCount":2,"failures":[]}
```

“每组重置”会在每个用例前清零引脚、寄存器和 RAM，同时保留 ROM，适合彼此独立的真值表；
“连续保留状态”让后一组继承前一组的状态，适合按时间顺序验证寄存器、计数器或状态机。

调试某一组时可执行“以 Hz 运行第 N 组测试用例”。编号从 `1` 开始，只运行这一组，
结果 JSON 中会包含 `caseNumber`，并且 `total` 为 `1`。连续保留状态模式下，单组测试会
继承电路当前状态；需要从初始状态复现某一步时，应选择“每组重置”，或先手动重置电路。

验证 Hz 表示每秒最多执行多少组用例，传播 Hz 表示每秒最多执行多少次受影响元件求值。
允许范围为 `0..1000`，其中 `0` 表示不限速。Hz 只在对应命令执行期间生效，不会启动后台
模拟；“模拟电路 N 步”也始终只执行 N 步。

## 示例二：使用 SWITCH 的 A 和 S

注册五个元件：

```text
dataInput    类型 LEVEL_INPUT
switchInput  类型 LEVEL_INPUT
switch1      类型 SWITCH
not1         类型 NOT
Level_Output 类型 LEVEL_OUTPUT
```

连接：

```text
dataInput.OUT -> switch1.A
switchInput.OUT -> switch1.S
switch1.OUT -> not1.A
not1.OUT -> Level_Output.IN
```

当 `switchInput=0` 时，`switch1.OUT` 恒为 `0`；当 `switchInput=1` 时，
`switch1.OUT` 等于 `dataInput`。不要尝试直接给 `switch1.OUT` 写值，它是计算结果。

## 两个输入的门怎么验证

以 AND 门为例，将 `inputA`、`inputB` 注册为 `LEVEL_INPUT`，将 `output` 注册为
`LEVEL_OUTPUT`，再注册 `and1` 并连接：

```text
inputA.OUT -> and1.A
inputB.OUT -> and1.B
and1.OUT -> output.IN
```

它们会自动成为两个验证输入和一个验证输出。验证数据：

```json
输入列表JSON：[
  {"inputA":0,"inputB":0},
  {"inputA":0,"inputB":1},
  {"inputA":1,"inputB":0},
  {"inputA":1,"inputB":1}
]
期望列表JSON：[
  {"output":0},
  {"output":0},
  {"output":0},
  {"output":1}
]
```

## “绑定”选项是什么意思

| 选项 | 行为 |
| --- | --- |
| 当前角色 | 元件绑定到当前执行积木的角色或克隆体 |
| 当前克隆体 | 只能由克隆体执行，并绑定到当前克隆体 |
| 角色或克隆体名称 | 元件绑定到下拉框中指定的其他目标 |
| 不绑定 | 元件只存在于逻辑引擎，不绑定 Scratch 目标 |

绑定目标被删除时，扩展会同时移除对应元件及其连线。普通关卡电路建议先使用“不绑定”；
只有需要让角色或克隆体代表一个可查询元件时才使用绑定。

## 传播、时钟和寄存器

模拟器平时不会自行运行。对于 NOT、AND、加法器等组合逻辑，使用“以 Hz 传播电路直到
稳定”。它不会触发寄存器或 RAM，适合手动调试组合电路；`0 Hz` 表示不限速。

寄存器和 RAM 需要时钟上升沿，推荐直接使用“让时钟输入产生脉冲”。也可以手动执行：

```text
先让 CLK = 0 -> 推进 Tick
再让 CLK = 1 -> 推进 Tick
```

只有 `0 -> 1` 会触发更新。保持 `CLK = 1` 连续推进不会重复写入；`1 -> 0` 也不会写入。

## 保存、复位和存储器

“重置电路运行状态”会保留元件和连线，清零端口、寄存器、RAM 和时钟状态；ROM 内容
会保留。“清空当前电路”则会删除全部元件和连线。

“当前电路的数据 JSON”提供三种结果：

- “结构”只保存元件、类型、位宽和连线，适合作为关卡模板，可重新导入。
- “运行状态”额外包含实时引脚和网络值，用于调试，不能作为导入数据。
- “完整快照”可重新导入，并保存 RAM、ROM、寄存器引脚和各时序元件上一次看到的
  CLK 电平，恢复后可以从原来的运行位置继续。

“导入电路结构或快照 JSON”会先完整校验并原子替换；任何数据无效时，原电路不会改变。
角色绑定和已载入的测试用例不属于存档，导入结构或快照成功后都会被清除。

每条结构连线保存为 `{ "id", "from", "to" }`。导入时 `id` 必须存在、非空且全局唯一；
元件类型必须使用菜单中的规范英文名称。前端应提供稳定线路 ID，方便把后端线路与绘制对象
对应起来。

正常运行时，存储器和其他元件通过引脚相互连接，并不是靠积木逐次读写：

```text
地址电路.OUT -> ROM.ADDR -> ROM.DATA -> 后续电路.IN

地址电路.OUT -> RAM.ADDR
数据电路.OUT -> RAM.DIN
写使能.OUT   -> RAM.WE
时钟输入.OUT -> RAM.CLK
RAM.DOUT     -> 后续电路.IN
```

ROM 必须有一种方式在关卡开始前装入程序，因此提供“将数据 JSON 写入 ROM”。RAM 的直接
读写、清空和存储器内容积木只用于关卡初始化、存档或调试；游戏电路运行时仍应通过
`ADDR`、`DIN`、`WE`、`CLK`、`DOUT` 引脚和连线访问。`LEVEL_INPUT` 的直接设值同理，它是
电路与关卡脚本之间的外部边界；时钟脉冲积木改变的是时钟输入元件，信号仍沿连线到达
REGISTER 或 RAM 的 `CLK` 引脚。

## 常见错误

- “逻辑门核心未开启”：先执行“启动电路模拟器”。
- “元件 ID 已存在”：同一个电路中 ID 必须唯一。
- “元件不存在”：重点检查 ID 的大小写是否与注册时完全相同。
- “端口不存在”：检查端口表和拼写。
- “线路 ID 已存在”：每条物理线路必须使用不同名称。
- “两个端口之间已存在线路”：同一端点对不能重复建立平行线路。
- “端口方向不兼容”：普通线路必须连接一个输出和一个输入；分支交汇请使用 `VIA.IO`。
- “端口位宽不匹配”：单线、2 位、4 位和 8 位总线不能直接混接。
- “多个驱动端”：同一根 Net 只能有一个输出端驱动。
- “逻辑震荡”：反馈回路达到根据电路规模计算的传播上限后仍无法稳定。
- 验证缺少元件：每组 JSON 都必须包含所有已登记的输入和输出 ID。

## 截图中“缺少 gate1”是什么意思

关系图中的 `components` 已经显示了 `gate1` 和 `gate3`，所以元件注册没有失败。
报错说的是“第 0 组输入 JSON 中缺少键 `gate1`”。因为 `gate1` 的类型是
`inputA` 的类型是 `LEVEL_INPUT`、`gate3` 的类型是 `LEVEL_OUTPUT`，它们注册时已自动成为验证接口。

验证数据必须使用这两个 ID：

```json
输入列表JSON：[{"gate1":0},{"gate1":1}]
期望列表JSON：[{"gate3":1},{"gate3":0}]
```

不能写 `LEVEL_INPUT` 或 `LEVEL_OUTPUT`，因为它们是元件类型，不是这个电路里的元件 ID。

## 多个 LEVEL_INPUT 和 LEVEL_OUTPUT

所有 `LEVEL_INPUT` 都会自动加入验证输入集合，所有 `LEVEL_OUTPUT` 都会自动加入验证输出
集合。假设电路注册了 `LEVEL_INPUT` `inputA`、`inputB` 和 `LEVEL_OUTPUT` `sum`、`carry`：

每组 JSON 必须同时提供所有已登记 ID：

```json
输入列表JSON：[{"inputA":0,"inputB":0},{"inputA":1,"inputB":1}]
期望列表JSON：[{"sum":0,"carry":0},{"sum":0,"carry":1}]
```

不需要额外的接口积木。删除某个 `LEVEL_INPUT` 或 `LEVEL_OUTPUT` 时，它也会自动离开集合。

## 多位逻辑门

注册积木的“位宽”是普通数字输入框，可以直接输入，也可以放入数字报告积木。
当前有效值为 `1`、`2`、`4`、`8`；其他值会返回明确错误。以下元件会采用该位宽：

- LEVEL_INPUT、LEVEL_OUTPUT、INPUT、OUTPUT、ALWAYS_ON、ALWAYS_OFF、SWITCH；
- NOT、AND、OR、NAND、NOR、XOR、XNOR；
- 3/4 输入 AND、OR、NAND、NOR、XOR、XNOR；
- MUX、AOI、OAI。

运算按每一个二进制位并行执行。例如 4 位 AND：

```text
A   = 12 = 1100
B   = 10 = 1010
OUT =  8 = 1000
```

相连端口的位宽必须一致。固定结构元件（转换器、加法器、寄存器、RAM、ROM）使用
元件定义中的固定位宽，不使用注册积木所选位宽。

### 过孔 VIA

`VIA` 用于告诉前端和后端某个线路交叉点确实需要电气交汇。它只有一个 `IO` 端口，不分
输入、输出或方向；任意数量的分支都连接到同一个端口：

```text
source.OUT ---- VIA.IO ---- sinkA.IN
                    |
                    +------ sinkB.IN
                    |
                    +------ otherVia.IO
```

没有放置 VIA 的几何交叉不会自动连接。后端不保存坐标，前端应仅在放置过孔时创建这些
`VIA.IO` 连线。

VIA 不使用添加积木中的位宽，也不能通过“将元件的位宽改为”设置。`IO` 会从相连的普通
端口自动推导 `1/2/4/8` 位；未连接或只连接其他 VIA 时查询结果是 `0`。一个 VIA 网络中
出现两种明确位宽会报错并保留原连接；多个真实输出汇合仍会报告多驱动错误。

普通输入口、输出口和 `VIA.IO` 都允许挂接多条线路。直接连接只允许 `output-input`；
`VIA.IO` 是无源端口，可连接 `output`、`input` 或另一个 `passive`。`input-input`、
`output-output`、端口连接自身、重复端点对、位宽冲突和多驱动都会返回本地化错误，而且
失败前后的线路和网络保持一致。

### 将 VIA 原子合并到端口

当前端把过孔位置替换成真实元件端口时，执行：

```text
将 VIA [via-1] 合并到端口 [not-1].[OUT]
```

后端会先在独立候选电路中找出所有连接 `via-1.IO` 的命名线路，把这些线路的 VIA 端改为
`not-1.OUT`，并保留每条线路原有的 `lineId`。如果某条线路的另一端已经是 `not-1.OUT`，
它会成为连接端口自身的零长度线路，因此会被删除。随后后端统一检查重复端点、端口方向、
位宽和整网多驱动；全部通过后才删除 VIA 并一次性提交，同时清除 VIA 的角色或克隆体绑定。

任意检查失败时，当前线路、VIA、绑定、引脚值、存储器内容和已载入测试数据都不会改变。
不要在 Scratch 中组合“删除线路 -> 删除 VIA -> 重新创建线路”，因为任一中间步骤失败都会
留下半完成拓扑。

### 命名线路与前端绘制

“连接线路”中的 `lineId` 在整个电路内唯一。留空会自动生成 `line-1`、`line-2` 等名称，
但前端通常应主动填写自己的对象 ID。“线路信息 JSON”和“全部线路 JSON”返回：

```json
{
  "id": "wire-input",
  "from": "Level_Input.OUT",
  "to": "not1.A",
  "width": 1,
  "value": 0,
  "netId": "net-3"
}
```

`width` 和 `value` 是线路所属电气网络的实时状态。`netId` 用于判断多条物理线路是否已经
属于同一个网络，但网络重建后可能变化；前端持久化和删除线路时必须使用稳定的 `id`。
后端不处理线路坐标、折点、颜色和几何交叉；没有 VIA 的视觉交叉不应创建后端连接。

这些可变宽度元件添加后还可以使用“将元件的位宽改为”重新设置。修改会保留能够兼容
新位宽的连线，并自动断开位宽不再一致的连线。把“端口的属性”切换为“位宽”，可以查询
任意数据脚或控制脚的实际位宽。

### 位宽支持总表

以下类型支持添加后在 `1 / 2 / 4 / 8` 位之间修改：

```text
LEVEL_INPUT  LEVEL_OUTPUT  INPUT  OUTPUT  SWITCH  ALWAYS_ON  ALWAYS_OFF
NOT  AND  OR  NAND  NOR  XOR  XNOR
3AND  3OR  3NAND  3NOR  3XOR  3XNOR
4AND  4OR  4NAND  4NOR  4XOR  4XNOR
MUX  AOI  OAI
```

`SWITCH.S` 和 `MUX.SEL` 始终为 1 位；其余数据脚采用所选宽度。固定结构如下：

| 类型 | 固定位宽 |
| --- | --- |
| `VIA` | `IO` 自动推导；未确定时为 0，不支持手动修改 |
| `HALF_ADDER` / `FULL_ADDER` | 所有引脚 1 位 |
| `ADDER4` | `A/B/SUM` 4 位，`CIN/COUT` 1 位 |
| `ADDER8` | `A/B/SUM` 8 位，`CIN/COUT` 1 位 |
| `SPLITTER` / `HUB` | 总线 8 位，`B0..B7` 1 位 |
| `SPLITTER2` / `HUB2` | 总线 2 位，`B0..B1` 1 位 |
| `SPLITTER4` / `HUB4` | 总线 4 位，`B0..B3` 1 位 |
| `CONVERTER_2_TO_8` | `IN0..IN1` 4 位，`OUT0..OUT7` 1 位 |
| `CONVERTER_8_TO_2` | `IN0..IN7` 1 位，`OUT0..OUT1` 4 位 |
| `CONVERTER_4_TO_2` | `IN0..IN3` 2 位，`OUT0..OUT1` 4 位 |
| `CONVERTER_2_TO_4` | `IN0..IN1` 4 位，`OUT0..OUT3` 2 位 |
| `CONVERTER_4_TO_8` | `IN0..IN3` 2 位，`OUT0..OUT7` 1 位 |
| `CONVERTER_8_TO_4` | `IN0..IN7` 1 位，`OUT0..OUT3` 2 位 |
| `REGISTER` | `D/Q` 8 位，`CLK` 1 位 |
| `ROM` | `ADDR/DATA` 8 位 |
| `RAM` | `ADDR/DIN/DOUT` 8 位，`WE/CLK` 1 位 |
| `BUS2_INPUT/OUTPUT` | 2 位 |
| `BUS4_INPUT/OUTPUT` | 4 位 |
| `BUS_INPUT/OUTPUT` | 8 位 |

## 全部积木的用途和结果

积木面板将端口查询整合为“端口的属性”，将结构、状态和快照整合为“当前电路的数据”，
避免重复接口。

### 模拟器

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 打开使用指南 | 查看离线说明和引脚表 | 打开弹窗，不修改电路 |
| 启动电路模拟器 | 创建计算引擎 | 优先启动 Worker，不支持时使用本地引擎；重复执行不会清空电路 |
| 关闭电路模拟器 | 释放引擎 | 删除运行电路与绑定并终止 Worker |
| 电路模拟器已启动？ | 查询引擎状态 | 返回布尔值，不修改状态 |
| 最近一次错误 | 查询最近失败 | 返回本地化错误；下一次成功操作会清空错误 |

### 电路搭建

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 清空当前电路 | 删除整个电路 | 删除元件、连线、测试数据和绑定 |
| 重置电路运行状态 | 重新初始化状态 | 保留拓扑；清零引脚、寄存器和 RAM，保留 ROM |
| 添加元件 | 创建指定类型实例 | 创建 ID 和位宽；VIA 忽略位宽并自动适应网络；`LEVEL_*` 自动登记为测试接口 |
| 删除元件 | 删除指定 ID | 同时移除相关连线、测试接口登记和绑定 |
| 存在元件？ | 检查 ID | 返回布尔值，不修改电路 |
| 连接线路 ID | 创建物理线路 | 保存稳定 ID 和端点；空 ID 自动生成；方向、位宽、重复端点或多驱动错误时原子失败 |
| 删除线路 ID | 删除指定物理线路 | 只删除该 ID，重建剩余网络并重新推导 VIA 位宽 |
| 将 VIA 合并到端口 | 用真实端口替换过孔 | 保留并改写 VIA 分支的线路 ID，删除零长度线路；完整校验通过后才删除 VIA 并提交 |
| 线路信息 JSON | 查询一条线路 | 返回 ID、端点、网络 ID、位宽和当前值 |
| 全部线路 JSON | 批量查询线路 | 按创建顺序返回数组，供前端检查和绘制 |
| 所有元件 ID JSON | 枚举元件 | 返回按添加顺序排列的 ID 数组 |
| 元件的端口列表 JSON | 查询全部引脚 | 返回名称、方向、位宽、当前值和连接状态 |
| 将元件的位宽改为 | 修改可变宽度实例 | 更新普通数据脚并断开不兼容连线；VIA 不支持手动修改 |
| 当前电路的结构/运行状态/完整快照 JSON | 保存或调试 | 结构用于模板；运行状态观察信号；快照还保存 RAM、ROM、寄存器和时钟历史 |
| 导入电路结构或快照 JSON | 恢复拓扑或完整状态 | 完整校验后原子替换；清除旧绑定和测试数据；失败时原电路不变 |

### 信号与时钟

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 将输入设为 | 设置一个外部输入 | 只修改输入源 `OUT`，不会自动传播 |
| 批量设置输入 JSON | 一次写多个输入 | 按 ID 写入给定输入，不会自动传播 |
| 端口的值/位宽 | 查询一个引脚属性 | 返回数字，不修改电路 |
| 批量读取端口 JSON | 查询多个引脚值 | 返回以 `元件ID.端口` 为键的对象 JSON |
| 以 Hz 传播电路直到稳定 | 按速率计算组合逻辑 | 每次受影响元件求值算一个节拍；传播到稳定或报告振荡，不采样寄存器/RAM 上升沿 |
| 让时钟输入产生脉冲 | 触发时序元件 | 每次先传播低电平再传播上升沿，最终 CLK 为 1 |
| 模拟电路若干步（高级） | 手动推进完整模拟 | 精确执行指定步数；每步先稳定组合逻辑，再采样当前 CLK 上升沿，结束后保持静止 |

### 测试与验证

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 载入测试用例 | 保存输入、期望向量和状态模式 | 先完整检查所有向量；每组重置适合独立用例，连续模式保留时序状态 |
| 以 Hz 运行第 N 组测试用例 | 单独验证一组 | 使用从 1 开始的编号；结果 `total=1`，连续模式继承当前状态 |
| 以 Hz 运行全部测试用例 | 执行完整验证 | 每组用例占一个节拍；按模式重置或继承状态，最终保留最后一组状态 |
| 测试结果 JSON | 读取报告 | 返回通过数、总数和每组失败差异 |

### 存储器初始化与调试

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 将数据写入 ROM | 关卡开始前初始化 ROM | 从偏移地址连续装入字节；运行时仍通过相连的 `ADDR`/`DATA` 引脚读取 |
| 读取存储器地址 | 后端调试 RAM/ROM | 返回指定字节，不代表电路引脚读取，也不改变引脚 |
| 设置 RAM 地址 | 后端初始化或调试 RAM | 立即修改字节；正常电路写入应使用相连的 `DIN/WE/CLK` 引脚 |
| 清空 RAM | 初始化单个 RAM | 256 字节变为 0；`DOUT` 在下一次传播后按 `ADDR` 更新 |
| 存储器内容 JSON | 存档或调试 RAM/ROM | 返回 256 字节数组 JSON，不替代运行时引脚连线 |

### 角色绑定

| 积木 | 用途 | 成功后的结果 |
| --- | --- | --- |
| 目标已绑定电路元件？ | 检查绑定 | 绑定记录和元件均存在时返回 `true` |
| 目标绑定元件的属性 | 查询绑定信息 | 返回 ID、类型、位宽或连线 JSON；未绑定时返回空文本 |
